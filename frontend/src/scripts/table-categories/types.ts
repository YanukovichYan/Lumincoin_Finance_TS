export type Operations = {
    amount: number
    category: string
    comment: string
    date: string
    id: number
    type: string
}

export type DefaultResponseType = {
    error: boolean
    message: string
}